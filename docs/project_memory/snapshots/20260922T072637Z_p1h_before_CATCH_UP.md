# 当前交接：原λ终点候选完整位移反转一阶下降预测

最新run mineral_prediction/p1g_finite_step/20260922T071328Z_p1g_candidate_fullstep
基准 228c812b43ae7ce9ed43b41d097201ca7da610c0，codex-refactor。仅2个临时副本单点位移，optimizer更新0，未追加训练。旧P1-G及全部诊断冻结。

# P1-G候选完整位移的单点诊断

结论：λ=0候选位移在48²和384²目标上都实际下降；原λ候选位移虽在两网格上一阶预测下降，完整位移却使两网格目标都上升。此处直接证实的是这一个冻结终点/候选位移的有限步长非线性效应。

| 模型 | 网格 | 一阶预测Δtotal | 实际Δtotal | 非线性余项 | 反转 |
|---|---|---:|---:|---:|---|
| empirical_P0 | 48² | -0.565319688 | -0.3710541847 | 0.1942655033 | False |
| empirical_P0 | 384² | -0.7386142868 | -0.5250368494 | 0.2135774374 | False |
| empirical_Plambda | 48² | -3.40636997 | 2.072712608 | 5.479082578 | True |
| empirical_Plambda | 384² | -3.739032419 | 1.665134796 | 5.404167215 | True |

反转指g·d<−1e-8但J(θ+d)−J(θ)>1e-8。余项=实际变化−一阶预测；它不是Hessian估计，不能据此把平滑曲率与激活非光滑性分开。只有预定α=0和α=1两个位置，没有半步、线搜索或选步长。

| 模型 | 网格 | 基线total | 探测点total | 实际Δdata | 实际Δpenalty |
|---|---|---:|---:|---:|---:|
| empirical_P0 | 48² | 2305.404845 | 2305.03379 | -0.3710541847 | 0 |
| empirical_P0 | 384² | 2306.289291 | 2305.764254 | -0.5250368494 | 0 |
| empirical_Plambda | 48² | 2317.966452 | 2320.039165 | 2.073007451 | -0.0002948430631 |
| empirical_Plambda | 384² | 2318.142098 | 2319.807232 | 1.665429639 | -0.0002948430631 |

原λ的经验数据项上升约2.0730/1.6654，正则仅下降约0.00029484，因此总目标同样上升；这不是Case F中“total下降却掩盖数据项恶化”的模式。两网格都上升，不能把该次候选位移的损失上升解释为只发生在粗网格的评价符号错误。

## 执行范围

每个模型只在独立临时副本上加一次前轮冻结的候选d，保存为diagnostic_only探测状态；这是两次临时参数位移，optimizer step=0，反向传播=0，没有延长P1-G训练轨迹，也没有产生用于后续训练的第2001步checkpoint。原模型、Adam状态、候选向量、历史结果和所有旧门槛不变。仅前向计算48²/384²目标，未新增更密网格、seed或训练对照。

49项验证通过，2315项历史文件哈希未变。基线目标复现已有归档，独立math.fsum/log/exp数据项及独立标量L2计算通过；实际副本位移与冻结d一致至1e-14，舍入后的g·d与原预测一致至1e-9。原始基线/探测网格分数、事件分数、探测模型state和有效位移均保存。

## 可以与不可以得出的结论

可以说：局部下降方向不保证这个完整候选位移下降；原λ这一终点给出具体反例。上轮“候选方向局部下降”仍然正确，本轮补充的是有限位移结果，不是推翻其方向导数。结果与有限步长过冲相容，但未区分曲率与非光滑机制。

不能说：原正则本身导致该问题、Adam整体失效、历史collapse原因已找到、或第二篇论文机制已成立。两个模型已处于不同参数状态，候选矩也不同，不能把这两个点的差别当作同一点正则干预。384²探测点的积分/导数收敛本轮未加密核验，故不作精确连续目标结论。没有计算本次位移后的teacher恢复指标，不把PPP变化叫恢复改善/恶化。

## 下一阶段边界（未执行）

本轮完成此前计划第1项，结束这一串终点局部诊断。下一项工作应先冻结单变量训练积分网格对照协议，再启动实验：固定data23/init1011、Adam/.003、2000更新、λ0/原λ，只改变训练积分网格；训练分辨率与独立评价网格必须事前固定。新发现作为预声明的有限步长非线性诊断线索，不同时改学习率/优化器/架构，也不继续扫描本次候选步长。本轮没有编制或启动该训练对照。

历史总状态：population恢复好而empirical恢复差，两者无观察到collapse；原λ在empirical固定终点KL较低但动态不稳定。两终点值层加密分别通过，梯度幅度受网格影响但无整体方向反转，候选Adam方向均局部下降；本次只有原λ完整位移使目标上升。原2000步stationarity未通过。报告随提交发布，远端状态以Git核对为准。
