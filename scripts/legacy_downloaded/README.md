# 下载的旧版代码：路径修正版

原始下载内容保留在 ../../archives/downloaded_code。此目录只提供已同步地化、断层和输出路径的副本，未重新训练这些旧模型。
ACAWLR_fault_ready.py 原本 FAULT_PATH=None 的设置保留；其他算法设置没有改变。
当前推荐矿产预测入口仍是 ../run_current_ppp.ps1，不要把这些旧算法当作本轮改进版。
