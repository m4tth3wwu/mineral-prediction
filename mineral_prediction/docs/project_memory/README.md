# Project memory

Read CATCH_UP.md, latest session, then protocol/provenance and raw artifacts. Memory is an evidence index, never permission to run experiments. Five distinct objects: density p, predictor g, coefficient field b, anchored decomposition beta/u/h, neural parameters theta. Decisions and run events are append-only; failures are retained. Current user instructions take precedence. Paths are relative to this repository; ../scripts is the actual external AST source.
